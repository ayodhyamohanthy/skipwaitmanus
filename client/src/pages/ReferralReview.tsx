import { ArrowLeft, Check, CheckCircle2, FileText, Loader2, MessageCircle, Sparkles, X } from "lucide-react";
import { useState } from "react";
import { Link, useRoute } from "wouter";
import DashboardLayout from "@/components/DashboardLayout";
import AvatarMark from "@/components/AvatarMark";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";

export default function ReferralReview() {
  const [, params] = useRoute("/referral-review/:id"); const { isAuthenticated } = useAuth(); const [decision, setDecision] = useState<"" | "approved" | "declined">(""); const [message, setMessage] = useState("");
  const { data: liveRequests } = trpc.referrals.listMine.useQuery(); const liveRequest = liveRequests?.find(item => String(item.id) === params?.id);
  const reviewMutation = trpc.referrals.review.useMutation({ onSuccess: data => setDecision(data.status) });
  const resolve = (kind: "approved" | "declined") => { if (!isAuthenticated) { startLogin(); return; } if (liveRequest) { reviewMutation.mutate({ requestId: liveRequest.id, decision: kind, message: message || undefined }); return; } setDecision(kind); };
  const candidate = liveRequest ? "A Bridge Job Seeker" : "Avery Morgan"; const jobTitle = liveRequest?.jobTitle || "Senior Product Designer"; const company = liveRequest?.company || "Northstar"; const pitch = liveRequest?.personalPitch || "I’m drawn to Northstar’s focus on useful, considered software. I’ve spent the last six years working on complex workflows for B2B teams, and I’d love the chance to bring that mix of product craft and systems thinking to this role.";
  if (decision) return <DashboardLayout title={decision === "approved" ? "Referral Request approved" : "Referral Request declined"} eyebrow="Referrer review complete"><div className="mx-auto max-w-xl py-14 text-center"><span className={`mx-auto grid h-16 w-16 place-items-center rounded-full ${decision === "approved" ? "bg-emerald-50 text-emerald-600" : "bg-slate-100 text-slate-600"}`}>{decision === "approved" ? <CheckCircle2 className="h-8 w-8" /> : <X className="h-8 w-8" />}</span><h2 className="mt-6 text-3xl font-semibold tracking-[-0.05em]">{decision === "approved" ? "You’re making a meaningful connection." : "Your decision has been shared respectfully."}</h2><p className="mt-4 text-sm leading-6 text-slate-600">{decision === "approved" ? "The Job Seeker has been notified that you’re ready to make an introduction." : "The Job Seeker will receive your note and can continue exploring other opportunities on Bridge."}</p><Link href="/dashboard" className="mt-8 inline-flex rounded-xl bg-violet-600 px-5 py-3 text-sm font-semibold text-white">Return to dashboard</Link></div></DashboardLayout>;
  return <DashboardLayout title="Review a Referral Request" eyebrow="Referrer review"><div className="mx-auto grid max-w-6xl gap-7 xl:grid-cols-[1.12fr_.88fr]"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_34px_rgba(28,25,55,0.045)] sm:p-8"><Link href="/dashboard" className="inline-flex items-center gap-1.5 text-xs font-bold text-slate-500 hover:text-violet-700"><ArrowLeft className="h-3.5 w-3.5" />Back to incoming requests</Link><div className="mt-6 flex gap-4"><AvatarMark initials={candidate.split(" ").map(value => value[0]).join("").slice(0, 2)} className="bg-violet-100 text-violet-700" size="lg" /><div><h2 className="text-2xl font-semibold tracking-[-0.05em]">{candidate}</h2><p className="mt-1 text-sm text-slate-600">Product Designer · San Francisco, CA</p><p className="mt-2 text-xs font-semibold text-emerald-700">Strong skills match for this role</p></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-[#F8F8FC] p-4"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Experience</p><p className="mt-3 text-sm font-semibold text-slate-900">6 years in product design</p><p className="mt-2 text-xs leading-5 text-slate-600">Led discovery and delivery for complex B2B product experiences.</p></div><div className="rounded-xl bg-[#F8F8FC] p-4"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Skills match</p><div className="mt-3 space-y-2">{[["Product Design",96],["Figma",92],["User Research",88]].map(([label, score]) => <div key={label as string}><div className="flex justify-between text-xs font-semibold text-slate-700"><span>{label as string}</span><span>{score as number}%</span></div><div className="mt-1 h-1.5 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-violet-500" style={{ width: `${score}%` }} /></div></div>)}</div></div></div><div className="mt-5 border-t border-slate-100 pt-5"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Personal pitch</p><blockquote className="mt-3 border-l-2 border-violet-300 pl-4 text-sm leading-7 text-slate-700">“{pitch}”</blockquote></div><div className="mt-5 flex items-center gap-2 text-sm font-semibold text-violet-700"><FileText className="h-4 w-4" />View full profile and resume</div></section><aside className="h-fit rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_34px_rgba(28,25,55,0.045)]"><div className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-violet-50 text-violet-700"><Sparkles className="h-4 w-4" /></span><div><p className="font-semibold text-slate-950">{jobTitle}</p><p className="text-xs text-slate-500">{company} · Hybrid</p></div></div><p className="mt-6 text-sm leading-6 text-slate-600">You decide whether to make this introduction. A clear note—yes or no—helps the Job Seeker understand what comes next.</p><label className="mt-6 block text-xs font-bold uppercase tracking-[0.14em] text-slate-500">Optional message<textarea value={message} onChange={event => setMessage(event.target.value)} placeholder="Add context or a helpful next step..." className="mt-2 min-h-28 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-normal normal-case tracking-normal outline-none transition focus:border-violet-500 focus:ring-4 focus:ring-violet-100" /></label><button disabled={reviewMutation.isPending} onClick={() => resolve("approved")} className="mt-5 flex w-full items-center justify-center gap-2 rounded-xl bg-violet-600 px-4 py-3 text-sm font-semibold text-white transition hover:bg-violet-700 disabled:opacity-60">{reviewMutation.isPending && <Loader2 className="h-4 w-4 animate-spin" />}<Check className="h-4 w-4" />{isAuthenticated ? "Approve Referral Request" : "Sign in to review"}</button><button disabled={reviewMutation.isPending} onClick={() => resolve("declined")} className="mt-3 flex w-full items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-700 transition hover:bg-slate-50"><X className="h-4 w-4" />Decline Referral Request</button>{reviewMutation.error && <p className="mt-3 text-sm font-medium text-rose-600">We couldn’t save this decision. Please try again.</p>}<div className="mt-6 flex items-start gap-2 border-t border-slate-100 pt-5 text-xs leading-5 text-slate-500"><MessageCircle className="mt-0.5 h-3.5 w-3.5 shrink-0 text-violet-600" />The Job Seeker will receive your decision and any message you choose to share.</div></aside></div></DashboardLayout>;
}
