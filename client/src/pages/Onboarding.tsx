import { ArrowRight, BriefcaseBusiness } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { Brand } from "@/components/Brand";
export default function Onboarding() { const [, go] = useLocation(); const [name,setName]=useState(""); const [role,setRole]=useState("Senior Product Designer"); const [company,setCompany]=useState("Orbital"); const continueFlow=()=>{localStorage.setItem("bridge-name",name||"Avery");localStorage.setItem("bridge-role",role);localStorage.setItem("bridge-company",company);go("/request")}; return <main className="min-h-screen bg-[#FCFCFE] px-6 py-6"><div className="mx-auto max-w-2xl"><Brand /><section className="mt-16 rounded-[28px] border border-slate-200 bg-white p-7 shadow-[0_20px_55px_rgba(31,25,72,.08)] sm:p-10"><p className="text-xs font-bold uppercase tracking-[.16em] text-violet-600">Start your request</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.05em]">What opportunity are you moving toward?</h1><p className="mt-3 text-sm leading-6 text-slate-600">Bridge needs just enough context to prepare a credible referral request.</p><div className="mt-8 space-y-5"><label className="block text-sm font-semibold">Your first name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Avery" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500" /></label><label className="block text-sm font-semibold">Target role<input value={role} onChange={e=>setRole(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500" /></label><label className="block text-sm font-semibold">Company<input value={company} onChange={e=>setCompany(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500" /></label></div><div className="mt-7 flex items-start gap-3 rounded-xl bg-violet-50 p-4 text-sm leading-6 text-violet-950"><BriefcaseBusiness className="mt-0.5 h-4 w-4 shrink-0" />Next, you’ll see one suggested Referrer and create the request in your own words.</div><button onClick={continueFlow} className="mt-8 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Continue <ArrowRight className="h-4 w-4" /></button></section></div></main>; }
