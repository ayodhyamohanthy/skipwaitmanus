import { ArrowRight, FileText, Mail, UserRoundCheck } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { Brand } from "@/components/Brand";
export default function Onboarding(){const[,go]=useLocation();const [name,setName]=useState("");const [targetUrl,setTargetUrl]=useState("");const [tokens]=useState(()=>Number(localStorage.getItem("bridge-tokens")||3));const begin=()=>{localStorage.setItem("bridge-name",name||"Avery");localStorage.setItem("bridge-target-url",targetUrl);localStorage.setItem("bridge-request-sent","true");localStorage.setItem("bridge-tokens",String(Math.max(0,tokens-1)));go("/request")};return <main className="min-h-screen bg-[#FCFCFE] px-6 py-6"><div className="mx-auto max-w-2xl"><Brand/><section className="mt-16 rounded-[28px] border border-slate-200 bg-white p-7 shadow-[0_20px_55px_rgba(31,25,72,.08)] sm:p-10"><p className="text-xs font-bold uppercase tracking-[.16em] text-violet-600">A clear, thoughtful ask</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.05em]">Get a referral without the awkwardness.</h1><p className="mt-3 max-w-lg text-sm leading-6 text-slate-600">Share the role you want. Your Referrer reviews the context and writes the hiring-manager email in their own voice.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><label className="block text-sm font-semibold">Your first name<input autoFocus value={name} onChange={e=>setName(e.target.value)} placeholder="Avery" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500"/></label><label className="block text-sm font-semibold">Target Role URL<input type="url" value={targetUrl} onChange={e=>setTargetUrl(e.target.value)} placeholder="Paste the job link" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500"/></label></div><button disabled={!targetUrl||tokens===0} onClick={begin} className="mt-7 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-5 py-3.5 text-sm font-semibold text-white disabled:opacity-40">Request referral <ArrowRight className="h-4 w-4"/></button><p className="mt-3 text-center text-xs text-slate-400">{tokens} free requests left · No email to write</p><div className="mt-8 grid gap-3 border-t border-slate-100 pt-6 sm:grid-cols-3"><div className="flex items-start gap-2"><UserRoundCheck className="mt-0.5 h-4 w-4 text-violet-600"/><p className="text-xs leading-5 text-slate-600"><strong className="block text-slate-900">Your context</strong>Start with the role.</p></div><div className="flex items-start gap-2"><FileText className="mt-0.5 h-4 w-4 text-violet-600"/><p className="text-xs leading-5 text-slate-600"><strong className="block text-slate-900">Resume, later</strong>Add it only if you want.</p></div><div className="flex items-start gap-2"><Mail className="mt-0.5 h-4 w-4 text-violet-600"/><p className="text-xs leading-5 text-slate-600"><strong className="block text-slate-900">Human email</strong>The Referrer writes it.</p></div></div></section></div></main>}
