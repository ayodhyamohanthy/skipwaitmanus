import { ArrowRight } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { Brand } from "@/components/Brand";
export default function Onboarding(){const[,go]=useLocation();const [name,setName]=useState("");const [targetUrl,setTargetUrl]=useState("");const continueFlow=()=>{localStorage.setItem("bridge-name",name||"Avery");localStorage.setItem("bridge-target-url",targetUrl);go("/request")};return <main className="min-h-screen bg-[#FCFCFE] px-6 py-6"><div className="mx-auto max-w-xl"><Brand/><section className="mt-20 rounded-[28px] border border-slate-200 bg-white p-7 shadow-[0_20px_55px_rgba(31,25,72,.08)] sm:p-9"><h1 className="text-3xl font-semibold tracking-[-.05em]">Request a referral</h1><p className="mt-2 text-sm text-slate-600">Paste the job link.</p><div className="mt-7 space-y-4"><label className="block text-sm font-semibold">Your name <span className="font-normal text-slate-400">(optional)</span><input value={name} onChange={e=>setName(e.target.value)} placeholder="Avery" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500"/></label><label className="block text-sm font-semibold">Job link<input type="url" value={targetUrl} onChange={e=>setTargetUrl(e.target.value)} placeholder="https://company.com/jobs/product-designer" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500"/></label></div><button disabled={!targetUrl} onClick={continueFlow} className="mt-7 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white disabled:opacity-40">Continue <ArrowRight className="h-4 w-4"/></button></section></div></main>}
