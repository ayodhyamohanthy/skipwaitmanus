import { ArrowRight, BriefcaseBusiness, Check, HeartHandshake, Sparkles, Target } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { Brand } from "@/components/Brand";
import { MvpOffer } from "@/components/MvpOffer";

type Role = "Job Seeker" | "Referrer";
const steps: Array<{ number: number; label: string }> = [{ number: 1, label: "Choose your side" }, { number: 2, label: "Name the target" }, { number: 3, label: "Open your referral map" }];

export default function Onboarding() {
  const [location, navigate] = useLocation(); const [role, setRole] = useState<Role>(location.includes("Referrer") ? "Referrer" : "Job Seeker"); const [step, setStep] = useState(1); const [target, setTarget] = useState("Senior Product Designer"); const [company, setCompany] = useState("Northstar");
  const finish = () => { localStorage.setItem("bridge-role", role); localStorage.setItem("bridge-target", target); localStorage.setItem("bridge-company", company); const context = `?target=${encodeURIComponent(target)}&company=${encodeURIComponent(company)}`; navigate(role === "Referrer" ? `/referral-review/mvp${context}` : `/referral-request/mvp${context}`); };
  return <div className="min-h-screen bg-[#FBFBFE] px-5 py-6 sm:px-8"><div className="mx-auto max-w-6xl"><Brand /><div className="mt-10 grid overflow-hidden rounded-[30px] border border-slate-200 bg-white shadow-[0_25px_70px_rgba(40,31,96,0.1)] lg:grid-cols-[.74fr_1.26fr]"><aside className="bg-slate-950 p-8 text-white sm:p-10"><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-300">Your referral path</p><h1 className="mt-5 text-balance text-4xl font-semibold tracking-[-0.055em]">A few clear inputs. One ready-to-send next move.</h1><div className="mt-12 space-y-5">{steps.map(item => <div key={item.number} className="flex items-center gap-3"><span className={`grid h-7 w-7 place-items-center rounded-full text-xs font-bold ${step >= item.number ? "bg-violet-500 text-white" : "bg-white/10 text-slate-400"}`}>{step > item.number ? <Check className="h-4 w-4" /> : item.number}</span><span className={`text-sm ${step >= item.number ? "font-semibold text-white" : "text-slate-400"}`}>{item.label}</span></div>)}</div><p className="mt-12 text-xs leading-5 text-slate-400">Bridge starts focused: one role, one path, one high-quality ask. Expand only when you need it.</p></aside><main className="p-7 sm:p-10">{step === 1 && <><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-600">Step 1 of 3</p><h2 className="mt-3 text-3xl font-semibold tracking-[-0.05em]">What are you here to do?</h2><p className="mt-3 text-sm leading-6 text-slate-600">Your first flow changes based on whether you need a path in or you’re offering one.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><button onClick={() => setRole("Job Seeker")} className={`rounded-2xl border p-5 text-left transition ${role === "Job Seeker" ? "border-violet-500 bg-violet-50 ring-2 ring-violet-100" : "border-slate-200"}`}><BriefcaseBusiness className="h-5 w-5 text-violet-700" /><p className="mt-5 font-semibold">I need a referral</p><p className="mt-2 text-sm leading-6 text-slate-600">Choose a serious target and build a credible introduction path.</p></button><button onClick={() => setRole("Referrer")} className={`rounded-2xl border p-5 text-left transition ${role === "Referrer" ? "border-violet-500 bg-violet-50 ring-2 ring-violet-100" : "border-slate-200"}`}><HeartHandshake className="h-5 w-5 text-violet-700" /><p className="mt-5 font-semibold">I give referrals</p><p className="mt-2 text-sm leading-6 text-slate-600">Review real requests with clear candidate context and human control.</p></button></div><button onClick={() => setStep(2)} className="mt-8 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white">Continue <ArrowRight className="h-4 w-4" /></button></>}{step === 2 && <><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-600">Step 2 of 3</p><h2 className="mt-3 text-3xl font-semibold tracking-[-0.05em]">What opportunity matters now?</h2><p className="mt-3 text-sm leading-6 text-slate-600">Start with the one role that would make a real difference. Bridge will help you reason from there.</p><div className="mt-8 space-y-4"><label className="block text-sm font-semibold text-slate-700">Target role<input value={target} onChange={event => setTarget(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 text-sm font-normal outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100" /></label><label className="block text-sm font-semibold text-slate-700">Company or team<input value={company} onChange={event => setCompany(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 text-sm font-normal outline-none focus:border-violet-500 focus:ring-4 focus:ring-violet-100" /></label></div><div className="mt-6 flex items-start gap-3 rounded-xl bg-violet-50 p-4"><Target className="mt-0.5 h-4 w-4 text-violet-700" /><p className="text-xs leading-5 text-violet-950">Copilot will use this target to map the available people, context, and best first ask. You can change it anytime.</p></div><div className="mt-8 flex gap-3"><button onClick={() => setStep(1)} className="rounded-xl border border-slate-200 px-4 py-3 text-sm font-semibold">Back</button><button onClick={() => setStep(3)} className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white">See my path <ArrowRight className="h-4 w-4" /></button></div></>}{step === 3 && <><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-600">Step 3 of 3</p><h2 className="mt-3 text-3xl font-semibold tracking-[-0.05em]">Your first referral path is ready to build.</h2><div className="mt-7 rounded-[22px] bg-[#F8F8FC] p-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-violet-100 text-violet-700"><Sparkles className="h-5 w-5" /></span><div><p className="font-semibold">{target}</p><p className="text-sm text-slate-500">{company} · Your focus for this path</p></div></div><p className="mt-4 text-sm leading-6 text-slate-600">You’ll see the strongest available Referrer, an AI explanation of the fit, and an editable Referral Request—all before you send anything.</p></div><div className="mt-6"><MvpOffer compact /></div><div className="mt-8 flex gap-3"><button onClick={() => setStep(2)} className="rounded-xl border border-slate-200 px-4 py-3 text-sm font-semibold">Back</button><button onClick={finish} className="inline-flex items-center gap-2 rounded-xl bg-violet-600 px-5 py-3 text-sm font-semibold text-white">Build my path free <ArrowRight className="h-4 w-4" /></button></div></>}</main></div></div></div>;
}
