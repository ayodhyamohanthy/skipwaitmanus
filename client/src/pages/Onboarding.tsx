import { ArrowRight, FileText, Mail, UserRoundCheck } from "lucide-react";
import { useEffect, useState } from "react";
import { useLocation } from "wouter";
import { Brand } from "@/components/Brand";
import { getJobSeekerTokens } from "@/lib/tokens";
import { readReferralDraft, saveReferralDraft } from "@/lib/pwaContinuity";

export default function Onboarding() {
  const [, go] = useLocation();
  const [name, setName] = useState(() => readReferralDraft()?.name || "");
  const [targetUrl, setTargetUrl] = useState(() => readReferralDraft()?.targetUrl || "");
  const [tokens] = useState(getJobSeekerTokens);
  useEffect(() => { saveReferralDraft({ name, targetUrl }); }, [name, targetUrl]);
  const begin = () => { localStorage.setItem("bridge-name", name || "Avery"); localStorage.setItem("bridge-target-url", targetUrl); saveReferralDraft({ name, targetUrl }); go("/request"); };

  return <main className="min-h-screen bg-[#F9F9FC] px-6 py-6"><div className="mx-auto max-w-3xl"><div className="flex items-center justify-between"><Brand /><span className="rounded-full border border-slate-200 bg-white px-3 py-1 text-[11px] font-semibold text-slate-500">{tokens} tokens included</span></div><section className="mt-14 rounded-[28px] border border-slate-200 bg-white p-7 shadow-[0_20px_60px_rgba(31,25,72,.08)] sm:p-10"><p className="text-xs font-bold uppercase tracking-[.16em] text-violet-600">Referral request</p><h1 className="mt-3 text-4xl font-semibold tracking-[-.055em] text-slate-950">Start with the opportunity.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-600">Share your name and the job link. Your resume comes next, then the Referrer reviews the request.</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><label className="block text-sm font-semibold text-slate-800">Your first name<input autoFocus autoComplete="given-name" value={name} onChange={(event) => setName(event.target.value)} placeholder="Avery" className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 font-normal outline-none transition focus:border-violet-500 focus:ring-4 focus:ring-violet-100" /></label><label className="block text-sm font-semibold text-slate-800">Target Role URL<input type="url" autoComplete="url" value={targetUrl} onChange={(event) => setTargetUrl(event.target.value)} placeholder="Paste the job link" className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 font-normal outline-none transition focus:border-violet-500 focus:ring-4 focus:ring-violet-100" /></label></div><button disabled={!targetUrl || tokens === 0} onClick={begin} className="mt-7 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-slate-950 px-5 py-3.5 text-sm font-semibold text-white transition hover:bg-violet-700 disabled:opacity-40">Continue <ArrowRight className="h-4 w-4" /></button><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4 text-xs"><span className="font-medium text-slate-500">1 token per request</span><span className="text-slate-400">More tokens · $1 each</span></div></section><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-4"><UserRoundCheck className="h-4 w-4 text-violet-600" /><p className="mt-3 text-sm font-semibold text-slate-900">Clear context</p><p className="mt-1 text-xs leading-5 text-slate-500">The exact role frames the request.</p></div><div className="rounded-2xl border border-slate-200 bg-white p-4"><FileText className="h-4 w-4 text-violet-600" /><p className="mt-3 text-sm font-semibold text-slate-900">Your evidence</p><p className="mt-1 text-xs leading-5 text-slate-500">Your resume is required next.</p></div><div className="rounded-2xl border border-slate-200 bg-white p-4"><Mail className="h-4 w-4 text-violet-600" /><p className="mt-3 text-sm font-semibold text-slate-900">A human ask</p><p className="mt-1 text-xs leading-5 text-slate-500">The Referrer writes the email.</p></div></div></div></main>;
}
