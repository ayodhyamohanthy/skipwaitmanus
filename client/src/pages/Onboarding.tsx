import { ArrowRight, Send } from "lucide-react";
import { useState } from "react";
import { useLocation } from "wouter";
import { Brand } from "@/components/Brand";
export default function Onboarding(){const[,go]=useLocation();const [name,setName]=useState("");const [targetUrl,setTargetUrl]=useState("");const continueFlow=()=>{localStorage.setItem("bridge-name",name||"Avery");localStorage.setItem("bridge-target-url",targetUrl);go("/request")};return <main className="min-h-screen bg-[#FCFCFE] px-6 py-6"><div className="mx-auto max-w-2xl"><Brand/><section className="mt-16 rounded-[28px] border border-slate-200 bg-white p-7 shadow-[0_20px_55px_rgba(31,25,72,.08)] sm:p-10"><p className="text-xs font-bold uppercase tracking-[.16em] text-violet-600">Request a referral</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.05em]">Which role would you like a referral for?</h1><p className="mt-3 text-sm leading-6 text-slate-600">Paste the exact job post. We’ll give the Referrer your context and resume, so they can decide and write the hiring-manager email in their own voice.</p><div className="mt-8 space-y-5"><label className="block text-sm font-semibold">Your first name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Avery" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500"/></label><label className="block text-sm font-semibold">Target Role URL<input type="url" value={targetUrl} onChange={e=>setTargetUrl(e.target.value)} placeholder="https://company.com/jobs/product-designer" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-violet-500"/></label></div><div className="mt-7 flex items-start gap-3 rounded-xl bg-violet-50 p-4 text-sm leading-6 text-violet-950"><Send className="mt-0.5 h-4 w-4 shrink-0"/>Next, attach your resume and send a simple request. The Referrer handles the referral email.</div><button disabled={!targetUrl} onClick={continueFlow} className="mt-8 inline-flex items-center gap-2 rounded-xl bg-slate-950 px-5 py-3 text-sm font-semibold text-white disabled:opacity-40">Continue to request <ArrowRight className="h-4 w-4"/></button></section></div></main>}
