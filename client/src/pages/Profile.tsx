import { Building2, Edit3, FileText, Loader2, MapPin, Plus, ShieldCheck, Sparkles } from "lucide-react";
import { useEffect, useState } from "react";
import DashboardLayout from "@/components/DashboardLayout";
import AvatarMark from "@/components/AvatarMark";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";

export default function Profile() {
  const { isAuthenticated } = useAuth(); const { data: storedProfile } = trpc.profile.mine.useQuery();
  const localRole = typeof window !== "undefined" ? localStorage.getItem("bridge-role") || "Job Seeker" : "Job Seeker"; const isReferrer = storedProfile?.accountType === "referrer" || (!storedProfile?.accountType && localRole === "Referrer"); const role = isReferrer ? "Referrer" : "Job Seeker";
  const [editing, setEditing] = useState(false); const [headline, setHeadline] = useState(""); const [bio, setBio] = useState(""); const [company, setCompany] = useState(""); const [capacity, setCapacity] = useState(2); const [skills, setSkills] = useState("");
  useEffect(() => { if (storedProfile) { setHeadline(storedProfile.headline || ""); setBio(storedProfile.bio || ""); setCompany(storedProfile.company || ""); setCapacity(storedProfile.referralCapacity ?? 2); setSkills(storedProfile.skills || storedProfile.expertise || ""); } }, [storedProfile]);
  const saveProfile = trpc.profile.save.useMutation({ onSuccess: () => setEditing(false) });
  const save = () => { if (!isAuthenticated) { startLogin(); return; } saveProfile.mutate({ accountType: isReferrer ? "referrer" : "job_seeker", headline, bio, company: company || undefined, currentTitle: isReferrer ? "Referrer" : "Job Seeker", skills: isReferrer ? undefined : skills, expertise: isReferrer ? skills : undefined, referralCapacity: isReferrer ? capacity : undefined }); };
  const skillList = (skills || (isReferrer ? "Product Design, Design Leadership, B2B SaaS, Hiring" : "Product Design, Design Systems, User Research, B2B SaaS, Figma")).split(",").map(item => item.trim()).filter(Boolean);
  const displayHeadline = headline || (isReferrer ? "Thoughtful introductions, grounded in context." : "A product designer who makes complex work feel calm.");
  const displayBio = bio || (isReferrer ? "I support people who bring craft, curiosity, and care to their work. I’m especially helpful for roles in product design, design systems, and B2B software." : "I bring six years of experience turning complicated workflows into simple, clear product experiences. I’m looking for a team that values deep customer understanding and careful execution.");
  return <DashboardLayout title="Profile" eyebrow={role}><div className="grid gap-7 xl:grid-cols-[.8fr_1.2fr]"><aside className="h-fit rounded-2xl border border-slate-200 bg-white p-6 text-center shadow-[0_14px_34px_rgba(28,25,55,0.045)]"><AvatarMark initials={isReferrer ? "MS" : "AM"} className="mx-auto bg-violet-100 text-violet-700" size="lg" /><h2 className="mt-4 text-xl font-semibold tracking-[-0.04em]">{isReferrer ? "Mira Shah" : "Avery Morgan"}</h2><p className="mt-1 text-sm text-slate-600">{isReferrer ? `Design Director${company ? ` at ${company}` : ""}` : "Product Designer"}</p><p className="mt-3 flex items-center justify-center gap-1.5 text-xs text-slate-500"><MapPin className="h-3.5 w-3.5" />{storedProfile?.location || "San Francisco, CA"}</p><button onClick={() => setEditing(true)} className="mt-6 inline-flex items-center gap-2 rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700 transition hover:bg-slate-50"><Edit3 className="h-3.5 w-3.5" />Edit profile</button><div className="mt-7 border-t border-slate-100 pt-6 text-left"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Profile visibility</p><div className="mt-3 flex items-center gap-2 text-sm font-medium text-emerald-700"><ShieldCheck className="h-4 w-4" />Visible to a relevant match</div></div></aside><section className="space-y-6"><article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_34px_rgba(28,25,55,0.045)]"><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-600">About</p><h2 className="mt-2 text-xl font-semibold tracking-[-0.04em]">{displayHeadline}</h2><p className="mt-4 text-sm leading-7 text-slate-600">{displayBio}</p></article><article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_34px_rgba(28,25,55,0.045)]"><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-600">{isReferrer ? "Expertise" : "Skills"}</p><div className="mt-4 flex flex-wrap gap-2">{skillList.map(skill => <span key={skill} className="rounded-full bg-violet-50 px-3 py-1.5 text-xs font-semibold text-violet-800">{skill}</span>)}<button onClick={() => setEditing(true)} className="inline-flex items-center gap-1 rounded-full border border-dashed border-violet-300 px-3 py-1.5 text-xs font-semibold text-violet-700"><Plus className="h-3.5 w-3.5" />Add</button></div></article>{isReferrer ? <article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_34px_rgba(28,25,55,0.045)]"><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-600">Referrer details</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-[#F8F8FC] p-4"><Building2 className="h-4 w-4 text-violet-600" /><p className="mt-3 text-sm font-semibold">{company || "Add your company"}</p><p className="mt-1 text-xs leading-5 text-slate-500">Design leader · Product organization</p></div><div className="rounded-xl bg-[#F8F8FC] p-4"><Sparkles className="h-4 w-4 text-violet-600" /><p className="mt-3 text-sm font-semibold">Referral capacity: {capacity}</p><p className="mt-1 text-xs leading-5 text-slate-500">You control how many thoughtful Referral Requests you can support.</p></div></div></article> : <article className="rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_34px_rgba(28,25,55,0.045)]"><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-600">Experience and resume</p><div className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-[#F8F8FC] p-4"><p className="text-sm font-semibold">Product Designer</p><p className="mt-1 text-xs text-slate-500">2021 — Present · Atlas Workflow</p><p className="mt-3 text-xs leading-5 text-slate-600">{storedProfile?.experience || "Add your experience so Referrers can understand the work you’ve done."}</p></div><div className="rounded-xl border border-dashed border-violet-300 p-4"><FileText className="h-4 w-4 text-violet-600" /><p className="mt-3 text-sm font-semibold">{storedProfile?.resumeUrl ? "Resume attached" : "Add your resume"}</p><p className="mt-1 text-xs text-slate-500">Profiles can include a resume link for relevant Referrers.</p><button onClick={() => setEditing(true)} className="mt-3 text-xs font-bold text-violet-700">Update profile</button></div></div></article>}</section></div>{editing && <div className="fixed inset-0 z-40 grid place-items-center overflow-y-auto bg-slate-950/30 p-5 backdrop-blur-sm"><div className="my-8 w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><p className="text-xs font-bold uppercase tracking-[0.14em] text-violet-600">Edit profile</p><h2 className="mt-2 text-xl font-semibold">Fine-tune your context.</h2><div className="mt-6 space-y-4"><label className="block text-sm font-semibold text-slate-700">Headline<input value={headline} onChange={event => setHeadline(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-500" /></label><label className="block text-sm font-semibold text-slate-700">About<textarea value={bio} onChange={event => setBio(event.target.value)} className="mt-2 min-h-24 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-500" /></label><label className="block text-sm font-semibold text-slate-700">{isReferrer ? "Expertise" : "Skills"}<input value={skills} onChange={event => setSkills(event.target.value)} placeholder="Separate skills with commas" className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-500" /></label>{isReferrer && <><label className="block text-sm font-semibold text-slate-700">Company<input value={company} onChange={event => setCompany(event.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-500" /></label><label className="block text-sm font-semibold text-slate-700">Referral capacity<input type="number" min="0" max="20" value={capacity} onChange={event => setCapacity(Number(event.target.value))} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-normal outline-none focus:border-violet-500" /></label></>}</div><div className="mt-6 flex gap-3"><button onClick={() => setEditing(false)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-700">Cancel</button><button onClick={save} disabled={saveProfile.isPending} className="inline-flex items-center gap-2 rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-60">{saveProfile.isPending && <Loader2 className="h-4 w-4 animate-spin" />}{isAuthenticated ? "Save profile" : "Sign in to save"}</button></div>{saveProfile.error && <p className="mt-3 text-sm font-medium text-rose-600">We couldn’t save your profile. Please try again.</p>}</div></div>}</DashboardLayout>;
}
