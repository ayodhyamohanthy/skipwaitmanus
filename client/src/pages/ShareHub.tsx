import { ArrowLeft, Copy, Link2, Share2 } from "lucide-react";
import { useAuth as useClerkAuth } from "@clerk/react";
import { useEffect, useMemo, useState } from "react";
import { Link, useLocation } from "wouter";
import { toast } from "sonner";
import { AccountMenu } from "@/components/AccountMenu";
import { Brand } from "@/components/Brand";
import { startLogin } from "@/const";

function open(url: string) { window.open(url, "_blank", "noopener,noreferrer"); }

export default function ShareHub() {
  const [, go] = useLocation();
  const { isLoaded, isSignedIn } = useClerkAuth();
  const [inviteCode, setInviteCode] = useState(""); const [email, setEmail] = useState(""); const [loading, setLoading] = useState(false);
  const origin = typeof window === "undefined" ? "https://skipwait.me" : window.location.origin;
  const link = useMemo(() => inviteCode ? `${origin}/start?invite=${encodeURIComponent(inviteCode)}` : "", [inviteCode, origin]);
  const message = useMemo(() => link ? `You found an opportunity! Now help your friends do the same.\n\nJoin skipwait.me with my link. We each get 1 extra referral credit when you join.\n\n${link}` : "", [link]);
  const emailInviteHref = useMemo(() => { const recipient = email.trim(); if (!link || !/^\S+@\S+\.\S+$/.test(recipient)) return ""; return `mailto:${recipient}?subject=${encodeURIComponent("You found an opportunity. Help a friend do the same.")}&body=${encodeURIComponent(message)}`; }, [email, link, message]);

  useEffect(() => { if (!isLoaded || !isSignedIn) return; setLoading(true); void fetch("/api/personal-invites/me", { credentials: "include" }).then(async response => response.ok ? response.json() : Promise.reject(new Error("Invite unavailable"))).then(data => setInviteCode(data.invite?.inviteCode ?? "")).catch(() => toast("We could not create your personal link. Try again.")).finally(() => setLoading(false)); }, [isLoaded, isSignedIn]);
  const copyLink = async () => { if (!link) return; try { await navigator.clipboard.writeText(link); toast("Personal link copied."); } catch { toast("Copy is unavailable in this browser."); } };
  const share = (channel: "x" | "facebook" | "linkedin" | "medium") => { if (!link) return; if (channel === "x") return open(`https://x.com/intent/post?text=${encodeURIComponent(message)}`); if (channel === "facebook") return open(`https://www.facebook.com/sharer/sharer.php?u=${encodeURIComponent(link)}`); if (channel === "linkedin") return open(`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(link)}`); void navigator.clipboard.writeText(message).then(() => toast("Invite copied. Paste it into a Medium story."), () => toast("Copy the personal link and paste it into Medium.")); open("https://medium.com/new-story"); };

  if (!isLoaded) return <main data-skipwait-screen="share-hub" className="h-dvh min-h-dvh overflow-hidden bg-slate-50 px-5 py-4 text-slate-950"><div className="mx-auto flex h-full max-w-xl flex-col"><header className="flex h-10 items-center"><Brand /></header><section className="flex min-h-0 flex-1 items-center"><div className="w-full"><div className="h-3 w-24 animate-pulse rounded bg-blue-100" /><div className="mt-3 h-16 w-4/5 animate-pulse rounded-xl bg-slate-200" /><div className="mt-4 h-28 animate-pulse rounded-xl border border-slate-200 bg-white" /><p className="mt-4 text-sm font-medium text-slate-500">Preparing your invite…</p></div></section></div></main>;
  if (!isSignedIn) return <main data-skipwait-screen="share-hub" className="h-dvh min-h-dvh overflow-hidden bg-slate-50 px-5 py-4 text-slate-950"><div className="mx-auto flex h-full max-w-xl flex-col"><header className="flex h-10 items-center"><Link href="/" className="inline-flex items-center gap-1 text-sm font-semibold text-slate-600"><ArrowLeft className="h-4 w-4" />Back</Link></header><section className="flex min-h-0 flex-1 items-center"><div className="w-full rounded-xl border border-slate-200 bg-white p-5 text-center shadow-sm"><span className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-blue-50 text-[#0B57D0]"><Share2 className="h-5 w-5" /></span><h1 className="mt-5 text-2xl font-semibold tracking-[-.04em]">Create your invite link.</h1><p className="mt-2 text-sm leading-6 text-slate-600">Sign in to share one personal link with a friend.</p><button onClick={() => startLogin()} className="mt-6 w-full rounded-lg bg-[#0B57D0] px-5 py-3 text-sm font-bold text-white">Sign in to continue</button></div></section></div></main>;

  return <main data-skipwait-screen="share-hub" className="h-dvh min-h-dvh overflow-hidden bg-slate-50 px-5 py-4 text-slate-950"><div className="mx-auto flex h-full max-w-xl flex-col"><header className="flex h-10 shrink-0 items-center justify-between"><Brand /><AccountMenu /></header><section data-skipwait-share-content="true" className="flex min-h-0 flex-1 flex-col justify-center py-2"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#0B57D0]">Your invite</p><h1 className="mt-1.5 text-[1.65rem] font-semibold leading-[.98] tracking-[-.055em]">You found an opportunity.<br />Help a friend do the same.</h1><p className="mt-2 text-[13px] leading-5 text-slate-600">When a friend joins with your link, you both receive one extra referral credit. Their 3 monthly credits stay included.</p></div><section className="mt-3 rounded-xl border border-slate-200 bg-white p-3 shadow-sm"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-slate-500">Personal link</p><div className="mt-2 flex items-center gap-2 rounded-lg bg-slate-50 p-2"><Link2 className="h-4 w-4 shrink-0 text-[#0B57D0]" /><code className="min-w-0 flex-1 truncate text-[11px] font-semibold text-slate-700">{loading ? "Creating your link…" : link}</code><button type="button" aria-label="Copy personal invite link" disabled={!link} onClick={() => { void copyLink(); }} className="rounded-md bg-white p-1.5 text-[#0B57D0] shadow-sm disabled:opacity-40"><Copy className="h-4 w-4" /></button></div><button type="button" disabled={!link} onClick={() => { void copyLink(); }} className="mt-2 w-full rounded-lg bg-[#0B57D0] px-4 py-2.5 text-sm font-bold text-white disabled:opacity-40">Copy link</button></section><section className="mt-2 rounded-xl border border-slate-200 bg-white p-3"><p className="text-[10px] font-bold uppercase tracking-[.14em] text-slate-500">Send by email</p><div className="mt-2 flex gap-2"><input aria-label="Friend email" type="email" inputMode="email" autoCapitalize="none" autoCorrect="off" spellCheck={false} value={email} onChange={event => setEmail(event.target.value)} placeholder="friend@gmail.com" className="min-w-0 flex-1 rounded-lg border border-slate-200 px-3 py-2 text-sm outline-none focus:border-[#0B57D0]" /><a href={emailInviteHref || undefined} onClick={event => { if (!emailInviteHref) { event.preventDefault(); toast("Enter one email address."); } }} aria-disabled={!emailInviteHref} className={`rounded-lg border border-blue-200 px-3 py-2 text-center text-sm font-bold ${emailInviteHref ? "text-[#0B57D0]" : "cursor-not-allowed text-slate-400"}`}>Invite</a></div></section><div className="mt-2 grid grid-cols-4 gap-2"><button aria-label="Share invite on X" onClick={() => share("x")} disabled={!link} className="rounded-lg border border-slate-200 bg-white py-2 text-sm font-bold text-slate-800 disabled:opacity-40">𝕏</button><button aria-label="Share invite on Facebook" onClick={() => share("facebook")} disabled={!link} className="rounded-lg border border-slate-200 bg-white py-2 text-sm font-bold text-[#0B57D0] disabled:opacity-40">f</button><button aria-label="Share invite on LinkedIn" onClick={() => share("linkedin")} disabled={!link} className="rounded-lg border border-slate-200 bg-white py-2 text-sm font-bold text-[#0B57D0]">in</button><button aria-label="Share invite on Medium" onClick={() => share("medium")} disabled={!link} className="rounded-lg border border-slate-200 bg-white py-2 text-sm font-bold text-slate-800 disabled:opacity-40">M</button></div><p className="mt-2 text-center text-[10px] leading-3.5 text-slate-500">Rewards apply once per verified new account. Self-invites and duplicate accounts are not eligible.</p></section><footer className="shrink-0 border-t border-slate-200 pb-[max(0.5rem,env(safe-area-inset-bottom))] pt-2"><button type="button" onClick={() => go("/requests")} className="w-full rounded-lg px-5 py-2.5 text-sm font-bold text-slate-600">Back to my requests</button></footer></div></main>;
}
