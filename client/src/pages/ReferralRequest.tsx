import { CheckCircle2, ChevronRight, FileText, Mail, Paperclip } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";
import { Brand } from "@/components/Brand";

export default function ReferralRequest() {
  const targetUrl = localStorage.getItem("bridge-target-url") || "your job link";
  const [tokens, setTokens] = useState(() => Number(localStorage.getItem("bridge-tokens") || 3));
  const [file, setFile] = useState<File | null>(null);
  const choose = (selectedFile: File | null) => {
    setFile(selectedFile);
    if (!selectedFile) return;
    localStorage.setItem("bridge-seeker-attachment-name", selectedFile.name);
    localStorage.setItem("bridge-seeker-attachment-type", selectedFile.type);
    const reader = new FileReader();
    reader.onload = () => localStorage.setItem("bridge-seeker-attachment-url", String(reader.result));
    reader.readAsDataURL(selectedFile);
  };
  return <main className="min-h-screen bg-[#F9F9FC] px-6 py-6"><div className="mx-auto max-w-4xl"><div className="flex items-center justify-between"><Brand/><span className="hidden rounded-full border border-slate-200 bg-white px-3 py-1 text-[11px] font-semibold text-slate-500 sm:block">Request active</span></div><div className="mt-14 grid gap-6 lg:grid-cols-[1.25fr_.75fr]"><section className="rounded-[30px] border border-slate-200 bg-white p-7 shadow-[0_24px_65px_rgba(31,25,72,.08)] sm:p-9"><div className="flex items-start gap-4"><span className="grid h-11 w-11 shrink-0 place-items-center rounded-2xl bg-emerald-50 text-emerald-600"><CheckCircle2 className="h-6 w-6"/></span><div><p className="text-xs font-bold uppercase tracking-[.16em] text-emerald-700">Request created</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.05em]">Your ask is with the Referrer.</h1><p className="mt-3 text-sm leading-6 text-slate-600">They have the role context and can now choose whether to make a personal introduction.</p></div></div><div className="mt-8 rounded-2xl border border-slate-100 bg-slate-50 p-4"><p className="text-[11px] font-bold uppercase tracking-[.14em] text-slate-500">Target role</p><p className="mt-2 truncate text-sm font-medium text-violet-700">{targetUrl}</p></div><div className="mt-6"><p className="text-sm font-semibold text-slate-900">One optional addition</p><p className="mt-1 text-sm text-slate-600">A resume can give the Referrer more context. You can add it now or skip it.</p><div className="mt-3 rounded-xl border border-dashed border-slate-300 p-3"><label className="flex cursor-pointer items-center gap-3 text-sm font-medium text-slate-600"><span className="grid h-8 w-8 place-items-center rounded-lg bg-violet-50 text-violet-600"><Paperclip className="h-4 w-4" /></span>{file ? <span className="flex items-center gap-2"><FileText className="h-4 w-4" />{file.name}</span> : "Add resume (optional)"}<input type="file" accept="application/pdf,image/png,image/jpeg" className="hidden" onChange={(event) => choose(event.target.files?.[0] || null)} /></label></div></div><Link href="/referrer" className="mt-7 inline-flex items-center gap-1 text-sm font-semibold text-violet-700">Preview Referrer view <ChevronRight className="h-4 w-4"/></Link></section><aside className="rounded-[30px] bg-[#121329] p-7 text-white sm:p-8"><p className="text-xs font-bold uppercase tracking-[.16em] text-violet-300">Request lifecycle</p><div className="mt-6 space-y-5"><div className="flex gap-3"><span className="grid h-7 w-7 place-items-center rounded-full bg-violet-400/15 text-violet-200">1</span><p className="text-sm leading-6"><strong className="block">Role received</strong><span className="text-slate-400">Your opportunity is ready for review.</span></p></div><div className="flex gap-3"><span className="grid h-7 w-7 place-items-center rounded-full bg-white/10 text-slate-300">2</span><p className="text-sm leading-6"><strong className="block">Referrer decides</strong><span className="text-slate-400">They review the context and choose how to help.</span></p></div><div className="flex gap-3"><span className="grid h-7 w-7 place-items-center rounded-full bg-white/10 text-slate-300">3</span><p className="text-sm leading-6"><strong className="block">Personal introduction</strong><span className="text-slate-400">If approved, they draft the hiring-manager email.</span></p></div></div><div className="mt-8 border-t border-white/10 pt-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-violet-300">Application tokens</p><div className="mt-2 flex items-center justify-between"><p className="text-3xl font-semibold">{tokens}</p><Link href="/premium" className="rounded-lg bg-white px-3 py-2 text-xs font-semibold text-slate-950">Add · $1 each</Link></div></div><div className="mt-6 flex items-center gap-2 rounded-xl bg-white/5 p-3 text-xs text-slate-300"><Mail className="h-4 w-4 text-violet-300"/>The Referrer owns the email, always.</div></aside></div></div></main>;
}
