import { ArrowRight, Building2, Search, SlidersHorizontal, UsersRound } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";
import DashboardLayout from "@/components/DashboardLayout";
import AvatarMark from "@/components/AvatarMark";
import { referrers as demoReferrers } from "@/lib/demoData";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";
export default function Community() { const { isAuthenticated } = useAuth(); const [search, setSearch] = useState(""); const [company, setCompany] = useState("All companies"); const [role, setRole] = useState("All roles"); const query = trpc.community.listReferrers.useQuery({}); const source = !isAuthenticated ? demoReferrers : (query.data || []).map((person, index) => ({ id: String(person.userId), name: person.name || "Bridge Referrer", initials: (person.name || "BR").split(" ").map(part => part[0]).join("").slice(0, 2), title: person.title || "Referrer", company: person.company || "Independent", expertise: person.expertise?.split(",").map(item => item.trim()).filter(Boolean) || [], location: person.location || "Remote", capacity: person.capacity ?? 0, accent: ["bg-violet-100 text-violet-700", "bg-sky-100 text-sky-700", "bg-rose-100 text-rose-700"][index % 3] })); const companies = Array.from(new Set(source.map(item => item.company))); const roles = Array.from(new Set(source.map(item => item.title))); const list = useMemo(() => source.filter(person => `${person.name} ${person.title} ${person.company} ${person.expertise.join(" ")}`.toLowerCase().includes(search.toLowerCase()) && (company === "All companies" || person.company === company) && (role === "All roles" || person.title === role)), [source, search, company, role]); return <DashboardLayout title="Community and Explore" eyebrow="Meet people who can offer context" actions={<Link href="/jobs" className="hidden rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 sm:inline-flex">Browse Job Listings</Link>}><div className="rounded-3xl bg-slate-950 px-6 py-9 text-white sm:px-9"><div className="max-w-2xl"><p className="text-xs font-bold uppercase tracking-[0.16em] text-violet-300">A considered community</p><h2 className="mt-3 text-balance text-3xl font-semibold tracking-[-0.055em] sm:text-4xl">Find the people behind the opportunity.</h2><p className="mt-4 text-sm leading-6 text-slate-300">Explore available Referrers by company and role. Every profile shows what they know, what they can support, and their current referral capacity.</p></div></div><div className="mt-7 flex flex-col gap-4 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 h-4 w-4 text-slate-400" /><input value={search} onChange={event => setSearch(event.target.value)} className="w-full rounded-xl bg-slate-50 py-3 pl-9 pr-3 text-sm outline-none focus:ring-2 focus:ring-violet-200" placeholder="Search by name, company, or expertise" /></div><select value={company} onChange={event => setCompany(event.target.value)} className="rounded-xl border border-slate-200 bg-white px-3 text-sm font-medium text-slate-700 outline-none"><option>All companies</option>{companies.map(value => <option key={value}>{value}</option>)}</select><select value={role} onChange={event => setRole(event.target.value)} className="rounded-xl border border-slate-200 bg-white px-3 text-sm font-medium text-slate-700 outline-none"><option>All roles</option>{roles.map(value => <option key={value}>{value}</option>)}</select><button className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 py-3 text-sm font-semibold text-slate-700"><SlidersHorizontal className="h-4 w-4" />More filters</button></div>{isAuthenticated && query.isLoading ? <div className="mt-7 rounded-2xl border border-slate-200 bg-white p-12 text-center text-sm text-slate-500">Loading available Referrers…</div> : list.length ? <div className="mt-7 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{list.map(person => <article key={person.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-[0_14px_34px_rgba(28,25,55,0.045)]"><div className="flex items-start justify-between"><AvatarMark initials={person.initials} className={person.accent} size="lg" /><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-bold text-emerald-700">{person.capacity} spots open</span></div><h2 className="mt-5 text-lg font-semibold tracking-[-0.04em] text-slate-950">{person.name}</h2><p className="mt-1 text-sm text-slate-600">{person.title}</p><div className="mt-5 flex items-center gap-2 border-y border-slate-100 py-4 text-xs font-medium text-slate-600"><Building2 className="h-3.5 w-3.5 text-violet-600" />{person.company}<span className="text-slate-300">·</span>{person.location}</div><div className="mt-5"><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-slate-400">Expertise</p><div className="mt-2 flex flex-wrap gap-1.5">{person.expertise.map(tag => <span key={tag} className="rounded-full bg-slate-100 px-2.5 py-1 text-[11px] font-semibold text-slate-600">{tag}</span>)}</div></div><Link href={`/referral-request/northstar-pd?referrer=${person.id}`} className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-violet-700">View referral path <ArrowRight className="h-4 w-4" /></Link></article>)}</div> : <div className="mt-7 rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center"><UsersRound className="mx-auto h-5 w-5 text-slate-400" /><p className="mt-3 font-semibold">{isAuthenticated ? "No Referrers are available for these filters yet." : "No Referrers match your search."}</p><p className="mt-2 text-sm text-slate-500">{isAuthenticated ? "Try returning as members share their availability." : "Try broadening your search."}</p><button onClick={() => { setSearch(""); setCompany("All companies"); setRole("All roles"); }} className="mt-3 text-sm font-bold text-violet-700">Clear search</button></div>}</DashboardLayout>; }
