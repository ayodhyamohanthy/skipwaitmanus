import { Check, Sparkles, X } from "lucide-react";
import { useState } from "react";

export function MvpOffer({ compact = false }: { compact?: boolean }) {
  const [open, setOpen] = useState(false); const [reserved, setReserved] = useState(false);
  return <><div className={`rounded-[24px] border border-violet-200 bg-[linear-gradient(135deg,#FCFBFF_0%,#F0EBFF_100%)] ${compact ? "p-4" : "p-6"}`}><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.16em] text-violet-700">Referral Ready Pass</p><p className={`mt-2 font-semibold tracking-[-0.04em] text-slate-950 ${compact ? "text-base" : "text-xl"}`}>One high-intent referral path, done thoughtfully.</p></div><span className="rounded-full bg-white px-2.5 py-1 text-xs font-bold text-violet-700 shadow-sm">$12</span></div><div className="mt-4 space-y-2 text-xs leading-5 text-slate-600"><p className="flex gap-2"><Check className="mt-0.5 h-3.5 w-3.5 shrink-0 text-violet-700" />AI-ranked Referrer paths for one role</p><p className="flex gap-2"><Check className="mt-0.5 h-3.5 w-3.5 shrink-0 text-violet-700" />Unlimited rewrites for your Referral Request</p><p className="flex gap-2"><Check className="mt-0.5 h-3.5 w-3.5 shrink-0 text-violet-700" />Private follow-through prompts</p></div><button onClick={() => setOpen(true)} className="mt-5 w-full rounded-xl bg-slate-950 px-4 py-2.5 text-sm font-bold text-white transition hover:bg-violet-700">See what’s included</button></div>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/30 p-5 backdrop-blur-sm"><div className="w-full max-w-md rounded-[26px] bg-white p-6 shadow-2xl"><button onClick={() => setOpen(false)} className="float-right rounded-lg p-1 text-slate-400 hover:bg-slate-100"><X className="h-4 w-4" /></button><span className="grid h-10 w-10 place-items-center rounded-2xl bg-violet-100 text-violet-700"><Sparkles className="h-5 w-5" /></span><p className="mt-5 text-xs font-bold uppercase tracking-[0.16em] text-violet-600">Referral Ready Pass</p><h3 className="mt-2 text-2xl font-semibold tracking-[-0.05em]">A paid path when the moment matters.</h3><p className="mt-3 text-sm leading-6 text-slate-600">Bridge is free to explore. This optional pass keeps the paid moment tied to a single concrete outcome: preparing a high-quality introduction for the role you care about.</p><div className="mt-5 rounded-xl bg-[#F8F8FC] p-4 text-sm font-semibold text-slate-900">$12 for one role-specific referral path</div><button onClick={() => setReserved(true)} className="mt-5 w-full rounded-xl bg-violet-600 px-4 py-3 text-sm font-bold text-white">{reserved ? "Pass reserved — we’ll notify you" : "Reserve launch access"}</button><p className="mt-3 text-center text-[11px] leading-5 text-slate-400">Prototype checkout moment. No payment is collected in this preview.</p></div></div>}</>;
}
