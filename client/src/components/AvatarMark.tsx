export default function AvatarMark({ initials, className = "bg-violet-100 text-violet-700", size = "md" }: { initials: string; className?: string; size?: "sm" | "md" | "lg" }) { const sizes = { sm: "h-8 w-8 text-[10px]", md: "h-10 w-10 text-xs", lg: "h-14 w-14 text-sm" }; return <span className={`grid shrink-0 place-items-center rounded-full font-bold ${sizes[size]} ${className}`}>{initials}</span>; }
