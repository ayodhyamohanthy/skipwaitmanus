export default function SectionHeading({ eyebrow, title, body, align = "left" }: { eyebrow?: string; title: string; body?: string; align?: "left" | "center" }) { return <div className={align === "center" ? "mx-auto max-w-2xl text-center" : "max-w-2xl"}>{eyebrow && <p className="mb-3 text-xs font-bold uppercase tracking-[0.18em] text-violet-600">{eyebrow}</p>}<h2 className="text-balance text-3xl font-semibold tracking-[-0.055em] text-slate-950 sm:text-4xl">{title}</h2>{body && <p className="mt-4 text-pretty text-base leading-7 text-slate-600">{body}</p>}</div>; }
