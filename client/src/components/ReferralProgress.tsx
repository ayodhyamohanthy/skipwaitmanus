import { getReferralProgress, referralStatusLabels, type ReferralStatus } from "@shared/referral";

export default function ReferralProgress({ status }: { status: ReferralStatus }) { const progress = getReferralProgress(status); return <div className="min-w-[150px]"><div className="mb-2 flex items-center justify-between gap-3"><span className="text-xs font-semibold text-slate-700">{referralStatusLabels[status]}</span><span className="text-[11px] font-semibold text-slate-400">{progress}%</span></div><div className="h-1.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-violet-500 transition-all" style={{ width: `${progress}%` }} /></div></div>; }
